import { ForecastStudio } from "@/components/finance-os/forecast/ForecastStudio";

export const metadata = { title: "NEXERA — Forecast Studio" };

export default function ForecastPage() {
  return <ForecastStudio />;
}
