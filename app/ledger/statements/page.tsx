import { StatementFactory } from "@/components/finance-os/statement-factory/StatementFactory";

export const metadata = { title: "NEXERA — Sales Statement Factory" };

export default function StatementsPage() {
  return <StatementFactory />;
}
