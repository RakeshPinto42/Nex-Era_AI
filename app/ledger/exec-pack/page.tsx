import { ExecutivePack } from "@/components/finance-os/exec-pack/ExecutivePack";

export const metadata = { title: "NEXERA — Executive Pack Generator" };

export default function ExecPackPage() {
  return <ExecutivePack />;
}
