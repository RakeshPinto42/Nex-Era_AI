import { RevRec } from "@/components/finance-os/rev-rec/RevRec";

export const metadata = { title: "NEXERA — Revenue Recognition Validator" };

export default function RevRecPage() {
  return <RevRec />;
}
