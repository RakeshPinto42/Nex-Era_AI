import { RevenueBridge } from "@/components/finance-os/revenue-bridge/RevenueBridge";

export const metadata = { title: "NEXERA — Revenue Bridge Builder" };

export default function RevenueBridgePage() {
  return <RevenueBridge />;
}
