import { MarginAnalysis } from "@/components/finance-os/margin/MarginAnalysis";

export const metadata = { title: "NEXERA — Margin Analysis" };

export default function MarginPage() {
  return <MarginAnalysis />;
}
